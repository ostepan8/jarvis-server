{
  "name": "turn_lights_blue",
  "description": "Set all connected lights to a color",
  "arguments": {"color": "blue"},
  "steps": [
    {"intent": "hue_command", "parameters": {"command": "Turn the lights {color}"}}
  ]
}
